package model.repository.impl;

import model.entity.Comment;
import model.repository.CommentRepository;

public class CommentRepositoryImpl extends RepositoryImpl<Comment, Integer> implements CommentRepository {
}
